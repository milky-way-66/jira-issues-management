# Secrets. Never commit this file — it holds the tracker token.
.env
.env.*
!.env.example

# Local scratch
*.tmp
.DS_Store

# Written by versions before 0.2.3, which generated the board as a file instead
# of serving it. Ignored so a leftover copy is never committed.
board.html

# Names one person, resolved from their own token. A shared value would give
# every teammate the same "My tasks".
.sync/identity.json

# Note what is deliberately NOT ignored:
#   .sync/base/   — the merge base. Committing it is what lets a three-way
#                   merge work across machines and across a fresh clone.
#   .sync/state.json — sync cursors.
